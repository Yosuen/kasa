import React from 'react';
import LogoR from './logoR';
import Nav from './nav';

const Header = () => {
    return (
        <div className='Header'>
            <LogoR/>
            <Nav/>
        </div>
    );
};

export default Header;